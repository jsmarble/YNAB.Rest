mode: Mainline
branches:
  main:
    regex: ^master$|^main$
    tag: ''
    increment: Patch
  branch:
    regex: ^[^\s\\]$
    source-branches: ['master', 'main', 'feature']
    tag: Alpha
    increment: Patch
ignore:
  sha: []
merge-message-formats: {}
